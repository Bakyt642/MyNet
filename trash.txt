<link type="text/css" href="{% static 'css/styles.css' %}" rel="stylesheet" />
boot version5.1
{% if category_slug == 0 %}
		<li class="selected">All Categories</li>
{% else %}





{% if category_slug == 0 %}
		<li class="selected">All Categories</li>
{% else %}
		<li class="selected"><a class="selected-a" href="{% url 'post_list' %}">All Categories</a></li>
{% endif %}


  {% for c in categories %}

                 {% if c.slug == category_slug %}
                       <li class="selected">{{ c.name }}</li>
                 {% else %}
                       <li class="selected"><a  class="selected-a"  href="{{ c.get_absolute_url }}">{{ c.name }}</a> </li>
                 {% endif %}

      {% endfor %}






      <li {% if not category %}class="selected"{% endif %}>
        <li class="selected"><a  href="{% url 'post_list' %}">All Categories</a></li>
      </li>

      {% for c in categories %}
        <li {% if category.slug == c.slug %}class="selected"{% endif %}>
           <li class="selected"><a   href="{{ c.get_absolute_url }}">{{ c.name }}</a> </li>
        </li>
      {% endfor %}
















{% endif %}

     {% if c.slug == category_slug %}
                       <li class="selected">{{ c.name }}</li>
                 {% else %}

                 {% endif %}

link type="text/css" href="{% static 'bootstap/css/bootstrap.min.css' %}" rel="stylesheet" />

   {% if category == 0 %}
         <li class="selected"> All </li>
      {% else %}
       {% endif %}
       {% for c in categories %}
        <li {% if category.slug == c.slug %}class="selected"{% endif %}>
          <a href="{{ c.get_absolute_url }}">{{ c.name }}</a>
        </li>
      {% endfor %}

<header class="p-3 bg-dark text-white">
    <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
          <svg class="bi me-2" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"></use></svg>
        </a>

        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
          <li><a href="#" class="nav-link px-2 text-secondary">Home</a></li>
          <li><a href="#" class="nav-link px-2 text-white">Features</a></li>
          <li><a href="#" class="nav-link px-2 text-white">Pricing</a></li>
          <li><a href="#" class="nav-link px-2 text-white">FAQs</a></li>
          <li><a href="#" class="nav-link px-2 text-white">About</a></li>
        </ul>

        <form class="col-12 col-lg-auto mb-3 mb-lg-0 me-lg-3">
          <input type="search" class="form-control form-control-dark" placeholder="Search..." aria-label="Search">
        </form>

        <div class="text-end">
          <button type="button" class="btn btn-outline-light me-2">Login</button>
          <button type="button" class="btn btn-warning">Sign-up</button>
        </div>
      </div>
    </div>
  </header>

  .left-chapters {
	margin: 0;
	background: #eee;
	border-right: 1px solid #ccc;
	min-height: 100%;
}


<div class="text-center p-4" style="background-color: rgba(0, 0, 0, 0.05);">
          © 2021 Copyright:
          <a class="text-reset fw-bold" href="https://mdbootstrap.com/">MDBootstrap.com</a>
        </div>






















